# Phase C.3 Security

Mitigations: IDOR→404, scope/classification/deny, no raw SQL/dynamic LINQ, list size limits, Unique OccurrenceKey + CycleId/FacilityId, RowVersion, no post-publish mutation, audit without full facility lists, catch-up capped, soft-delete filters, no client-only auth.
