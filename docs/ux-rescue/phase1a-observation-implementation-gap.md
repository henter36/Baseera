# فجوة تنفيذ Phase 1A — Observation Workspace Foundation

هذا الملف تنفيذي فقط، ولا يكرر `docs/ux-rescue/observation-workspace-gap-analysis.md`. يوثّق فقط قرارات هذه الشريحة (Vertical Slice) بناءً على مراجعة الكود الفعلي بعد دمج PR #147/#148.

**اكتشاف جوهري**: خلاف انطباع تدقيق Phase 0، الـBackend لهذا المجال متقدم فعليًا — عقود `GET /notes/workspace` و`GET /notes/{id}/workspace` موجودتان بالفعل مع Scope filtering وRedaction وPagination/Sort/Filter allowlists وAllowedActions. الفجوة الحقيقية أضيق بكثير مما بدا، ومركّزة في: فجوة واحدة في `BuildAllowedActions` (إغلاق الملاحظة غير موجود كإجراء مسموح رغم وجود الـEndpoint)، حقول DTO ميتة تغذّي تبويبات Placeholder دائمة، عدم استخدام `FacilityUnitId` عند الإنشاء، وغياب التكامل بين Facility Workspace وإنشاء ملاحظة.

الجدول أدناه هو **Pre-Phase-1A baseline** محفوظ كتحليل تاريخي؛ القرارات النهائية بعد التنفيذ موثقة في `phase1a-observation-route-transition.md` و`phase1a-observation-compliance-ledger.md`.

| المتطلب | العقد الحالي | قابل لإعادة الاستخدام | الفجوة | القرار |
| --- | --- | --- | --- | --- |
| قائمة ملاحظات مصفّاة بالنطاق | `GET /notes/workspace` → `NoteWorkspaceQueryService.ListAsync` → `NoteQueryService.ListAsync` (Scope عبر `INoteScopeService.FilterQueryableAsync`، فلترة/فرز/صفحات مع Allowlist) | نعم بالكامل | لا يوجد | Keep — إعادة استخدام مباشر |
| تفاصيل ملاحظة موحّدة | `GET /notes/{id}/workspace` → `NoteWorkspaceDetailDto` (Header+Assignments+CorrectiveActions+Attachments+Timeline+AllowedActions) | نعم، مع تعديل | يحتوي حقول `Resources`/`Decisions`/`Links` دائمة الفراغ (لا Domain فعلي) تغذّي تبويبات Placeholder | Restructure — إزالة الحقول الثلاثة من الـDTO والـUI |
| Server-authored allowed actions | `NoteWorkspaceQueryService.BuildAllowedActions` تُعيد قائمة نصية مسطّحة (لا `primary`/`blocked`/`blockedReason`) | جزئيًا | **لا يوجد إجراء "إغلاق" (`VERIFY_CLOSURE`) في القائمة رغم وجود Endpoint `/verify-closure` فعليًا وصلاحية `Notes.VerifyClosure` معرَّفة** | Fix — إضافة `VERIFY_CLOSURE` + بناء تصنيف Primary/Secondary في طبقة العرض (لا حاجة لعقد DTO جديد، يكفي دالة اشتقاق في الواجهة من نفس القائمة + الحالة) |
| Assign/Reassign inline | `POST /{id}/assign` (`AssignNoteRequest`) + `GET /{id}/eligible-assignees` موجودان في الـBackend، لكن العميل لا يستدعي `eligible-assignees` إطلاقًا، والواجهة تفرض تنقّلاً كاملًا لـ`/notes/:id` عند ASSIGN/REASSIGN | نعم للـBackend، لا للواجهة | لا Client method لـ`eligible-assignees`/`eligible-reviewers`؛ لا نموذج Assign مضمَّن | Fix — إضافة Client methods + نموذج Assign Inline (Panel) |
| Verify/Close inline | `POST /{id}/verify-closure` (`CloseNoteRequest`: Reason+ClosureSummary+RowVersion) موجود، مستدعى فعليًا في `api.notes.verifyClosure` لكن غير مستخدَم في الواجهة (غائب من `ACTION_LABELS`/`INLINE_ACTIONS`) | نعم | لا نموذج Close مضمَّن (يحتاج حقل ClosureSummary إضافي لا يكفيه حقل Reason وحده) | Fix — نموذج Close Inline مخصص |
| إنشاء ملاحظة بوراثة سياق السجن | `POST /notes` (`CreateNoteRequest`) + `NoteScopeService.ResolveIntakeAsync` يتحقق فعليًا من: نشاط الموقع، تبعية الموقع للمنطقة، `orgScope.CanAccess`، وقفل ملف الإدخال (`UserNoteIntakeProfile`) | نعم لمعظم المنطق | (أ) لا مسار في الواجهة يمرر `facilityId` من Facility Workspace عند الإنشاء. (ب) `NoteCommandService.CreateDraftAsync` **يفرض `FacilityUnitId = null` دائمًا** ويتجاهل `request.FacilityUnitId` كليًا رغم أن `NoteScopeService.Supported` و`OrganizationalScopeShape`/`OrganizationalScopeEntityGuard` تدعمان `ScopeType.FacilityUnit` فعليًا لبقية العمليات | Fix — تفعيل مسار الوحدة (FacilityUnit) في `CreateDraftAsync` (تفعيل قدرة بنية تحتية موجودة، وليس تعديل Domain جديدًا)؛ إضافة لوحة إنشاء داخل Facility Workspace تمرر `facilityId`/`facilityUnitId` من السياق |
| منع تلاعب العميل بـFacilityId | `ResolveIntakeAsync` يرفض موقعًا خارج نطاق المستخدم أو مخالفًا لقفل ملف الإدخال | نعم | لا يوجد اختبار Integration مؤكِّد لهذا السلوك تحديدًا لسيناريو "الإنشاء من Facility Workspace" | إضافة اختبار تكامل صريح (rejection test) |
| السابق/التالي | لا يوجد Endpoint مخصص | جزئيًا — الصفحة المحمَّلة حاليًا (≤50 عنصرًا) تكفي لحساب الجار مباشرة | لا حاجة لعقد Backend جديد | بناء منطق Prev/Next في الواجهة من نافذة الصفحة الحالية المفلترة/المرتّبة نفسها |
| Route/URL موحَّد | `/notes/workspace` موجود، يدعم بالفعل: search/status/severity/regionId/facilityId/overdueOnly/requiresMyAction/requiresRouting/page/sortBy/sortDesc/noteId | نعم لمعظم الحالة | ينقصه: `facilityUnitId`، `owner` (Department)، `assignee` (منفصل عن requiresMyAction)، `noteType`، `due` (دمج overdue/dueSoon)، `view` (Presets)، `section` (تبويب التفاصيل)، `source` | Restructure — توسيع مجموعة المعاملات دون تكرار تمثيل نفس الحالة |
| توافق Routes قديمة | `/notes` و`/notes/workspace` يعرضان **نفس المكوّن بالفعل** اليوم (لا حاجة لـFeature flag بينهما)؛ `/notes/:id` و`/notes/:id/edit` صفحات Legacy منفصلة تمامًا (`NoteDetailPage`/`NoteEditPage`) | جزئيًا | لا آلية Resolver بعد تتحكم عبر Feature Flag فيما إذا يُفتح `/notes/:id` كصفحة Legacy أو كـDeep-link لـWorkspace | بناء Route Resolver + `VITE_OBSERVATION_WORKSPACE_V2` |
| مكوّنات مشتركة | `WorkspaceShell.tsx` يحوي بالفعل: `WorkspaceEmpty/Error/Loading/Unauthorized`, `WorkspaceFilterBar`, `WorkspaceWidgetContainer`, `MasterDetailWorkspaceLayout` (غير مستخدَم حتى الآن) | نعم جزئيًا | التسمية/الموقع لا يطابق الاتفاقية المطلوبة (`shared/workspaces/`)؛ لا يوجد `WorkspaceCommandHeader`/`WorkspaceActionBar`/`WorkspaceStateView` موحَّدة | نقل/تكييف ما هو موجود إلى `shared/workspaces/`، لا إعادة كتابة من الصفر |
| Legacy fallback | `NotesListPage.tsx` كان غير موجَّه له Route في Pre-Phase-1A baseline، وأصبح الآن مسجّلًا خلف `VITE_OBSERVATION_WORKSPACE_V2=false` | نعم كـFallback مقصود | Archive restoration remains available through the legacy fallback during Phase 1A. | Legacy fallback retained intentionally behind `VITE_OBSERVATION_WORKSPACE_V2=false`. Archive restoration remains available through the legacy fallback during Phase 1A. |
| Escalate inline | لا Endpoint لتصعيد ملاحظة بعينها فوريًا (مؤكَّد من PR #148) | لا | يبقى خارج النطاق صراحة | لا تنفيذ — يبقى fallback لصفحة الإعدادات |
| Accept assignment | لا Endpoint/Command مستقل لـ"قبول التكليف" في هذا الـDomain اليوم (`AcceptedAtUtc` حقل DTO لكن بلا مسار كتابة) | لا | خارج نطاق الإثبات المطلوب لهذه الشريحة | لا تنفيذ — تُوثَّق كـNot Applicable |

## ملخص القرار التنفيذي

**لا حاجة لبناء Read Model جديد بالكامل.** العمل الفعلي هو: (1) إصلاحات دقيقة على `NoteWorkspaceQueryService`/`NoteCommandService` الموجودتين، (2) توسيع عميل الواجهة بميثودات ناقصة (`eligibleAssignees`/`eligibleReviewers`)، (3) بناء واجهة أمامية جديدة تستهلك نفس العقود الموجودة بتغطية أوسع (Assign/Close inline, Prev/Next, إنشاء من Facility Workspace, Route resolver, مكوّنات مشتركة).
