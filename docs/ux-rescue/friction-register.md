# سجل نقاط الاحتكاك — UX Rescue Phase 0

كل بند أدناه مستخرج من الكود الفعلي والرحلات الموثَّقة في `current-user-journeys.md` — لا توصيات شكلية أو تفضيلات لونية.

| ID | المجال | الشاشة/Route | الدور | المهمة | المشكلة | الخطورة | التكرار | الأثر | السبب الجذري | القرار |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| F-01 | تشغيل النماذج | `/form-campaigns/:campaignId/edit` | ناشر النموذج | تعديل حملة موجودة | المكوّن المعروض (`FormCampaignWizardPage`) لا يقرأ `:campaignId` إطلاقًا؛ يعرض معالج إنشاء فارغًا بدل تحميل بيانات الحملة الحالية — تعديل حملة **غير ممكن فعليًا** | Critical | كل محاولة تعديل حملة | فقدان بيانات محتمل، فشل مهمة كاملة | Data/state mismatch | Replace completely — بناء تدفق تعديل حقيقي يحمّل الحملة أولًا |
| F-02 | تشغيل النماذج | `/form-campaigns/:campaignId/targeting`, `/schedule` | ناشر النموذج | الوصول لخطوة استهداف/جدولة مباشرة | Routes معرَّفة لكن غير قابلة للوصول من أي رابط، وبنفس عطل F-01 إن وُصِل إليها يدويًا | High | نادر (لا رابط يقود إليها) لكن الخطر كامن | ثقة منخفضة بمسارات غير مُختبَرة عمليًا | Route fragmentation | Remove/redirect |
| F-03 | التنظيم/المناطق | `/regions` | مدير منطقة | مقارنة السجون واتخاذ قرار | جدول مسطّح بلا أي رابط أو إجراء على أي صف — الرحلة تنتهي عند القائمة | Critical | يومي (محاولة) | دور كامل (مدير منطقة) غير مخدوم بالتطبيق | Entity-oriented IA / Missing aggregation | Restructure → يتطلب #12 |
| F-04 | مركز قرار السجن | عام | مدير سجن | الوصول لمساحة عمل سجنه | لا يوجد رابط شريط جانبي إلى `/workspaces/facilities/:facilityId`؛ نقطة الدخول الوحيدة عمود "مركز القرار" داخل جدول `/facilities` | High | يوميًا لكل دخول | خطوة إضافية خفية لأهم شاشة في التطبيق | Hidden next action | Restructure — إضافة رابط شريط جانبي مباشر |
| F-05 | الملاحظات | `ObservationWorkspacePage` | مسؤول عمليات | تكليف/إعادة تكليف ملاحظة | إجراء ASSIGN/REASSIGN يفرض تنقّلاً كاملاً إلى `/notes/:id` (لا نموذج تكليف مضمَّن) | High | لكل تكليف (يومي) | فقدان سياق القائمة والفلاتر أثناء إجراء متكرر جدًا | Context loss | Merge into Workspace (#143) |
| F-06 | الملاحظات | `ObservationWorkspacePage` | مدير سجن/معتمِد | اعتماد إغلاق ملاحظة (`verifyClosure`) | غير ضمن `INLINE_ACTIONS`، يفرض تنقّلاً كاملاً لـ`/notes/:id` رغم كونه إجراءً حرجًا متكررًا | High | لكل إغلاق | فقدان سياق أثناء إجراء اعتماد حساس | Context loss | Merge into Workspace |
| F-07 | الملاحظات | `FacilityWorkspacePage` | جميع أدوار السجن | إنشاء ملاحظة أثناء استعراض سجن معيّن | لا يوجد رابط "ملاحظة جديدة" في الملف إطلاقًا؛ المستخدم يغادر تمامًا للشريط العلوي، وحتى عند وصوله لـ`/notes/new` لا يرث `facilityId` (الحقول تُشتق من سياق تكليف المستخدم نفسه) | Critical | يوميًا | انقطاع كامل للسياق التنظيمي عند أهم إجراء إنشاء في المجال الأكثر استخدامًا | Missing aggregation / Context loss | Merge into Workspace — أولوية قصوى ضمن #143 |
| F-08 | الملاحظات/الإجراءات | `NoteDetailPage`, `CorrectiveActionDetailPage`, `ObservationWorkspacePage` tabs | مسؤول ملاحظات | رفع مرفق دليل | 3 أسطح مختلفة لنفس المفهوم: رفع+قائمة (تفاصيل الملاحظة)، قائمة فقط بلا رفع (تفاصيل الإجراء)، قراءة فقط تمامًا (تبويب Workspace) | Medium | لكل توثيق دليل | ارتباك حول أين يمكن فعليًا رفع دليل | Duplicate controls | Restructure — توحيد مكوّن مرفقات واحد بقدرات متسقة |
| F-09 | الملاحظات/التصعيد | لا يوجد | مسؤول متابعة | تصعيد ملاحظة/إجراء متأخر فورًا | لا زر "صعّد الآن" في أي مكان — التصعيد إما آلي بالكامل أو تشغيل يدوي عام من `/settings/escalations` لا يستهدف عنصرًا بعينه | High | عند كل حاجة تصعيد يدوي عاجل | تأخير قرار حرج بسبب غياب إجراء مباشر | Hidden next action | Restructure — ضمن نطاق دمج التصعيد بـ#143/#146 |
| F-10 | التصعيد | `EscalationsSettingsPage` وأخواتها الأربع | مسؤول تصعيد | إدارة السياسات | لا تحقق صلاحية داخل مكوّنات الصفحات الأربع كلها؛ الحماية الوحيدة هي إخفاء رابط الشريط الجانبي | Medium | دائم (بنيوي) | اعتماد كامل على حماية مسار واحدة (Nav) بدل دفاع متعدد الطبقات | Permission ambiguity | Polish — إضافة تحقق صلاحية داخل الصفحات نفسها |
| F-11 | Facility Workspace | `/facilities/:facilityId/occupancy\|resources\|workforce` | متخصصو المجالات | التنقل من الـWorkspace لوحدة فرعية | Route كامل منفصل، لا يحمل فلتر الفترة الزمنية من الـWorkspace، رابط عودة يدوي فقط عبر `?section=` | High | يوميًا، متكرر جدًا | إعادة ضبط الفلاتر يدويًا في كل انتقال | Context loss / Route fragmentation | Merge into Workspace |
| F-12 | بنية الواجهة العامة | كل الصفحات تقريبًا | جميع المطورين/المستخدمين بشكل غير مباشر | أي شاشة قائمة/جدول/فلتر | لا مكتبة مكونات مشتركة (`shared/` يحتوي 4 دوال تنسيق فقط)؛ كل صفحة تُعيد تنفيذ الجدول والفلاتر والبطاقات من الصفر | High | بنيوي، يؤثر كل شاشة | تضخم الكود، تفاوت السلوك بين شاشات تؤدي نفس المهمة | Duplicate controls | Restructure — استخراج مكونات مشتركة عند إعادة بناء #143/#144/#145 |
| F-13 | بنية الواجهة العامة | `WorkspaceShell.tsx` مقابل `FacilityWorkspacePage.tsx` | جميع مستخدمي مساحات العمل | ترويسة/تخطيط مساحة العمل | مكوّن `WorkspaceShell`/`WorkspaceHeader` جاهز وموحّد لكن **يُستخدم في `ReferenceWorkspacePage` (مرجعي/تطويري) فقط**؛ مساحة العمل الحقيقية الوحيدة تُعيد تنفيذ ترويسة/تخطيط منفصلين بالكامل | High | بنيوي | ازدواجية صيانة، وخطر انحراف بصري/وظيفي بين المساحتين مستقبلاً | Duplicate controls | Restructure — تبنّي `WorkspaceShell` الفعلي في Facility Workspace |
| F-14 | منشئ النماذج | `DesignerPropertiesPanel` | منشئ النموذج | بناء شرط ظهور أو صيغة حساب | لا توجد أي واجهة لإدخال `visibilityCondition`/`formula` رغم وجود نموذج بيانات كامل (`FormConditionGroup`/`FormFormulaNode`) ومحرك تقييم فعلي يعمل في المعاينة | Critical | لكل نموذج يحتاج منطقًا شرطيًا (متكرر في نماذج التشغيل الحقيقية) | يجب تعديل JSON الخام خارج الواجهة لأي منطق شرطي — يتناقض تمامًا مع هدف #144 "دون كتابة تعبيرات حرة" | Inadequate progressive disclosure (قدرة متقدمة غائبة كليًا لا مخفية فقط) | Replace completely — بناء Condition/Formula builder ضمن #144 |
| F-15 | منشئ النماذج | `FormDesignerPage` | منشئ النموذج | حماية العمل غير المحفوظ | لا `beforeunload` ولا حاجز تنقّل (`useBlocker`) في أي مكان؛ نافذة تعرّض فعلية مدتها 800ms بين كل تغيير وحفظه، والانتقال أثناء حالة `error`/`conflict` غير محجوب | High | نادر الحدوث لكن الأثر تدميري عند وقوعه | فقدان تصميم غير محفوظ بصمت | Weak error recovery | Restructure — إضافة حارس تنقّل + تحذير إغلاق التبويب |
| F-16 | منشئ النماذج | Form مقابل FormVersion | منشئ/مراجع النموذج | فهم حالة النموذج الحقيقية | كيانان بحالتين منفصلتين تمامًا (`FormDefinitionStatus` و`FormVersionStatus`) بصفحتي مراجعة منفصلتين، بلا أي تفسير للمستخدم عن العلاقة بينهما؛ يمكن أن يكون النموذج "معتمَد" بينما إصداره لا يزال "مسودة" دون تحذير متبادل | High | مفاهيمي، يظهر تأثيره عند أي تفاوت حالة فعلي | ارتباك حول "أين حالتي الحقيقية؟" | Entity-oriented IA | Restructure — دمج الحالتين في عرض واحد مفهوم ضمن #144 |
| F-17 | منشئ النماذج | `FormVersionsPage`/`FormDesignerPage` | منشئ النموذج | فهم أن الإصدار مقفَل | القفل يُعرَض كحجب كامل للصفحة ("لا يمكن تعديل هذا الإصدار...") بدل عرض الـCanvas بوضع قراءة فقط | Medium | لكل إصدار مقفَل يُفتَح بالخطأ | تجربة مفاجئة (صفحة فارغة) بدل تعلّم بصري بأن المحتوى مقفَل | Weak error recovery | Polish — عرض Canvas للقراءة فقط بدل حجب كامل |
| F-18 | منشئ النماذج | `FormTemplatesPage` | منشئ النموذج | بدء نموذج من قالب | ينفّذ `window.location.assign` (إعادة تحميل كامل للمتصفح) — الوحيد من نوعه في كامل التطبيق بدل تنقّل SPA عادي؛ كما أن مسار القوالب منفصل تمامًا عن `/forms/new` بلا أي ربط بينهما | Medium | لكل إنشاء من قالب | فقدان حالة التطبيق (إعادة تحميل كاملة)، ونقطتا دخول منفصلتان لنفس الهدف | Duplicate controls | Restructure — دمج القوالب كخطوة بداية داخل استوديو موحّد |
| F-19 | تشغيل النماذج | `/form-response-reviews` → `/form-responses/:id/review` | مراجع الاستجابة | مراجعة استجابة ثم الانتقال للتالية | لا تنقّل تالٍ/سابق إطلاقًا؛ عودة إجبارية للقائمة الكاملة (50 عنصرًا) بعد كل قرار مراجعة | High | لكل استجابة، عالي التكرار اليومي لهذا الدور | مضاعفة عدد التنقلات الفعلي لمهمة تتكرر عشرات المرات يوميًا | Hidden next action | Restructure |
| F-20 | تشغيل النماذج | مصطلحات "رد/استجابة/استحقاق" و"تكليف/استحقاق/تعيين" | كل أدوار تشغيل النماذج | فهم المفهوم نفسه عبر شاشات مختلفة | 3 كلمات عربية مختلفة لنفس مفهوم "Response" و3 كلمات مختلفة لنفس مفهوم "Assignment" عبر صفحات المجال نفسه، بالإضافة لتسريب رؤوس أعمدة إنجليزية خام ("Occurrence", "Target hash") وقيم Hash تقنية بلا شرح | Medium | يظهر في كل شاشة بالمجال | حمل معرفي إضافي لربط المفاهيم المتطابقة | Inconsistent terminology | Polish — توحيد المصطلح ضمن مسرد واحد |
| F-21 | تشغيل النماذج | Facility Workspace → لوحة معاينة نموذج | مدير سجن/مستجيب | فتح استجابة أو مراجعتها من داخل السجن | اللوحة السياقية تفتح على لوحة التزام السجن (`/form-compliance/facilities/:id`) وليس مباشرة صفحة الاستجابة/المراجعة — قفزة إضافية غير مباشرة | Medium | لكل محاولة وصول سريع من الـWorkspace | خطوة يدوية إضافية لإيجاد العنصر الفعلي بعد الوصول للوحة الالتزام | Context loss | Restructure |
| F-22 | المخاطر | لا يوجد Route | مسؤول المخاطر | إدارة سجل المخاطر (قائمة/تفاصيل/إنشاء) | عقد Backend كامل (`api.risks.*`) بلا أي صفحة مستقلة؛ العمل الوحيد الممكن هو من داخل لوحة سياقية واحدة في Facility Workspace بلا رابط "صفحة كاملة" (يُعيد `null` صراحة) | Critical | يومي لهذا الدور بأكمله | دور كامل محدود بمساحة عمل واحدة صغيرة بلا قائمة/بحث/إنشاء حقيقي | Missing aggregation | Restructure — يستحق Issue تنفيذية مستقلة (خارج نطاق #143-#146 المباشر) |
| F-23 | المشاريع/الخطط/القرارات | Facility Workspace | مدير سجن | استعراض مشاريع/خطط/قرارات السجن | لا عقد Backend لهذه المجالات إطلاقًا؛ تُعرض بنفس مكوّن "فجوة" (`DomainUnavailableSection`) المستخدَم أيضًا لمجالات حقيقية بلا بيانات — لا تمييز بين "لا بيانات بعد" و"الميزة غير موجودة إطلاقًا" | Medium | دائم لهذه الأقسام الثلاثة | توقع مستخدم مخالف للواقع (يظن أن البيانات فارغة مؤقتًا) | Data/state mismatch | Polish — رسالة مميزة "قيد التخطيط" بدل رسالة الفراغ العامة |
| F-24 | لوحة المتابعة | `/dashboard` مقابل `/workspaces/facilities/:facilityId` | مسؤول متابعة/مدير | فهم وضع سجن محدد | بيانات متداخلة جزئيًا (ملاحظات/إجراءات/تصعيد) بين الصفحتين، لكن **لا رابط عبور واحد بينهما** — Dashboard لا يربط أبدًا بمركز القرار حتى عند عرض بيانات سجن بعينه | Medium | يوميًا لهذا الدور | ازدواجية عرض بلا تكامل تنقّل، يربك أي مستخدم يتنقل بين الاثنين | Duplicate controls | Restructure — إما دمج أو ربط Drill-down مباشر |
| F-25 | عابر للمجالات | كل الصفحات | جميع الأدوار على الأجهزة الصغيرة | إنجاز أي مهمة على Tablet/Mobile | لا تنقّل موبايل مخصص إطلاقًا (لا Hamburger، لا Bottom nav، لا منطق JS مرتبط بحجم الشاشة) — كل الاستجابة CSS فقط؛ زر "رجوع" الخاص بالموبايل معرَّف لكن غير مستخدَم في أي صفحة فعلية | High | يؤثر أي مستخدم ميداني (مستجيب النموذج، مسؤول غرفة التحكم، منفّذ الإجراء) | تجربة سطح مكتب "مُصغَّرة" لا تجربة موبايل مصمَّمة | Mobile incompatibility | Restructure — قرار تصميم صريح مطلوب لكل مجال (دعم كامل أو وضع مراجعة فقط، موثَّق) |
| F-26 | عابر للمجالات | كل نقاط الإنشاء/التعديل | كل الأدوار | تنفيذ عملية على عدة عناصر دفعة واحدة | لا يوجد أي تحديد متعدد أو "تنفيذ على المحدَّد" في أي شاشة بالتطبيق | Medium | يتزايد الاحتياج مع نمو حجم البيانات (خصوصًا مراجعة الاستجابات والتذكيرات) | عمل يدوي متكرر عنصرًا بعنصر بلا بديل جماعي | Missing aggregation | Restructure — يحتاج بناء نمط جديد بالكامل ضمن #145 |
| F-27 | الإجراءات التصحيحية | `CorrectiveActionScopeService` | (بنيوي، غير مرئي للمستخدم مباشرة) | فرض عزل النطاق التنظيمي | لا يستدعي `IOrganizationalScopeService` مباشرة؛ يعتمد كليًا وبشكل غير مباشر على نطاق الملاحظة الأصل (`noteScope.CanAccess`) | Medium | بنيوي، خطر كامن لا حادثة مسجَّلة | هشاشة معمارية: أي تغيير مستقبلي في منطق نطاق الملاحظات دون مراجعة الإجراءات التصحيحية معه قد يكشف تسربًا | Data/state mismatch | Polish — توثيق الاعتماد صراحة أو ربطه مباشرة بخدمة النطاق العامة |
| F-28 | عقود الخادم | Middleware الاستثناءات | (بنيوي) | توحيد شكل استجابة الخطأ للعميل | 3 أشكال Payload مختلفة لأخطاء التحقق/التعارض عبر المجالات (شكل عام، شكل FluentValidation بحقل `errors`، شكل خاص بالنماذج بحقل `issues`/`payload` خام) | Medium | بنيوي، يؤثر أي معالجة أخطاء عميل جديدة | صعوبة بناء معالجة أخطاء موحّدة في العميل مستقبلاً | Weak error recovery | Polish — توحيد الشكل تدريجيًا عند لمس كل مجال |
| F-29 | عقود الخادم | DTOs عدة مجالات | (بنيوي) | معرفة الإجراءات المسموحة من الواجهة | لا حقل `AllowedActions` في DTOs الإجراءات التصحيحية/الإشغال/القوى البشرية رغم وجوده في الملاحظات (Workspace variant فقط) والمخاطر والموارد والعهد الحساسة والنماذج | Medium | بنيوي | الواجهة تُضطر لاستنتاج الإجراءات المسموحة من الحالة/الصلاحية يدويًا لهذه المجالات، بخطر عدم تطابق مع قواعد الخادم الفعلية | Permission ambiguity | Restructure — تعميم النمط عند إعادة بناء كل مجال |
| F-30 | Facility Workspace | تبديل القسم (`?section=`) | جميع مستخدمي الـWorkspace | استخدام زر "رجوع" في المتصفح | كل تبديل قسم يُنشئ إدخال تاريخ منفصلاً (`replace:false`)؛ زر الرجوع يتنقّل عبر الأقسام السابقة قبل مغادرة الصفحة فعليًا، بخلاف التوقع الشائع لزر "رجوع" (مغادرة الصفحة الحالية) | Low-Medium | يحدث عند كل استخدام لزر رجوع المتصفح داخل الـWorkspace | سلوك تنقّل غير متوقَّع، قد يُربك لا يكسر وظيفة | Data/state mismatch | Polish — يستحق قرارًا صريحًا (الإبقاء أو التحويل لـ`replace:true`) |

## ملخص حسب السبب الجذري

| السبب الجذري | عدد البنود |
| --- | --- |
| Context loss | 5 (F-05, F-06, F-07, F-11, F-21) |
| Missing aggregation | 4 (F-03, F-07 يتقاطع, F-22, F-26) |
| Duplicate controls | 5 (F-08, F-12, F-13, F-18, F-24) |
| Data/state mismatch | 4 (F-01, F-23, F-27, F-30) |
| Hidden next action | 3 (F-04, F-09, F-19) |
| Weak error recovery | 3 (F-15, F-17, F-28) |
| Permission ambiguity | 2 (F-10, F-29) |
| Entity-oriented IA | 2 (F-03 يتقاطع, F-16) |
| Route fragmentation | 1 (F-02) |
| Inadequate progressive disclosure | 1 (F-14) |
| Inconsistent terminology | 1 (F-20) |
| Mobile incompatibility | 1 (F-25) |

**إجمالي نقاط الاحتكاك الموثَّقة: 30** (يفوق الحد الأدنى المطلوب وهو 20)، منها **5 مصنَّفة Critical** (F-01, F-03, F-07, F-14, F-22) تستحق أولوية تنفيذية فورية ضمن #143/#144/#146.
