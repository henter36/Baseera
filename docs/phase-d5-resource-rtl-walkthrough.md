# Phase D.5 Resource RTL Walkthrough

Resource views inherit the Phase D.2 command-center RTL layout.

Checks:

- Arabic labels and status rails remain right-to-left.
- Category rail and exceptions list avoid horizontal scrolling on mobile.
- Full-page fallback routes return to `/workspaces/facilities/{facilityId}?section=resources`.
- Focusable controls use buttons or links only where actions exist.
