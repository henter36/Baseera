# Phase D.2 Screenshots

Expected captures:
- Desktop 1440px.
- Desktop with context panel open.
- Tablet.
- Mobile overview.
- Mobile detail.
- Critical state.
- Stable state.
- Partial-data state.
- Empty state.

Local capture note:
- The implementation was validated through automated frontend rendering tests.
- Browser screenshots require a local API/session with seeded facility data. The local API process did not reach the listening state during this run, so screenshots must be attached from the PR review environment or a fully running local stack.

