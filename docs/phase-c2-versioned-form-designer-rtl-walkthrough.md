# Phase C.2 RTL Walkthrough (manual)

1. Open `/forms/{id}/versions` — RTL table and actions.
2. Open designer `/forms/{id}/versions/{versionId}/edit` — palette right/left under `dir=rtl`, page tabs, properties panel.
3. Drag handle and keyboard ↑↓ reorder fields; labels remain Arabic.
4. Preview desktop/tablet/mobile — frame stays RTL.
5. Validation panel messages Arabic.
6. Conflict banner after forced 409 — reload CTA Arabic.
7. Templates page `/form-templates` RTL list.
